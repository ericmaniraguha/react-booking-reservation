const initialState = {
  products: [
    {
      id: 1,
      title: 'Shirt',
      category: 'cloths',
    },
  ],
};
export const productReducer = (state, action) => {
  switch (type) {
    case ActionTypes.SET_PRODUCTS:
      return state;
    default:
      return state;
  }
};
